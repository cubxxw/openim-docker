interface:
  display_name: "Openim Docker"
  short_description: "Repo-specific patterns and workflows for openim-docker"
  default_prompt: "Use the openim-docker repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true